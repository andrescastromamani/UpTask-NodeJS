doctype html
html(lang="en")
  head
    include ../partials/head
  body.login
    .form-container 
      h1 Forgot Password - UpTask
      p Please enter your email address and we will send you a link to reset your password.
      //pre=vardump(name)
      form#form.box.login-box(method="POST" action="/auth/forgot")
        .field
          label(for="email") Email
          input#email(type="email" name="email" placeholder="Email")
        .field.send
          input#submit(type="submit" value="Reset" class="button")
        .field.actions
          a(href="/auth/register") Do you remember your password?